unzip oracle-xe-11.2.0-1.0.x86_64.rpm.zip
cd Disk1/
sudo apt-get install -y alien libaio1 unixodbc
sudo alien --scripts oracle-xe-11.2.0-1.0.x86_64.rpm      : 한참걸림
sudo dpkg -i oracle-xe_11.2.0-2_amd64.deb
sudo ln -s /usr/bin/awk /bin/awk
sudo mkdir /var/lock/subsys
sudo touch /var/lock/subsys/listener
sudo chmod 777 /var/lock/subsys/listener
sudo umount -l tmpfs
sudo rm -rf /dev/shm
sudo mkdir /dev/shm
sudo mount -t tmpfs shmfs -o size=4096m /dev/shm   : RAM사이즈랑 같게
sudo /etc/init.d/oracle-xe configure
sudo /etc/init.d/oracle-xe start
sudo vi /etc/bash.bashrc
	. /u01/app/oracle/product/11.2.0/xe/bin/oracle_env.sh
source /etc/bash.bashrc
vi ~/.bashrc  
	export ORACLE_HOME=/u01/app/oracle/product/11.2.0/xe  
	export ORACLE_SID=XE  
	export NLS_LANG=`$ORACLE_HOME/bin/nls_lang.sh`  
	export ORACLE_BASE=/u01/app/oracle  
	export LD_LIBRARY_PATH=$ORACLE_HOME/lib:$LD_LIBRARY_PATH  
	export PATH=$ORACLE_HOME/bin:$PATH  
source ~/.bashrc
cd /etc/init.d
sudo vi 아이디
	#!/bin/bash
	su - 아이디
	. /u01/app/oracle/product/11.2.0/xe/bin/oracle_env.sh
	/etc/init.d/oracle-xe start
sudo update-rc.d 아이디 defaults
sudo mkdir /oradata
sudo chmod 777 /oradata
sqlplus
	id : system
	pw : 비번