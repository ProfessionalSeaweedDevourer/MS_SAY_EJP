// 별도의 form 없이, input에 쓴 내용을 그대로 reqParam으로 요청
function doReq(){

    var nInput = document.getElementById("nameInput");
    var aInput = document.getElementById("ageInput");

    var n = nInput.value;
    var a = aInput.value;

    // location.href="https://google.com" // get 요청

}